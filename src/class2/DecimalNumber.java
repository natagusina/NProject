package class2;

public class DecimalNumber {
    public static void main(String[] args) {
        /*
        if we want store decimal numbers in computer's memory using java we can use following types float
        decimal
         */
        float myNumberBox=10.5f;
        System.out.println(myNumberBox);
        double largeDecimalBox=122234566.234;
        /*
        if we want to store characters(&,s,4\0 we can use char datatype
         */
        char letter='c';
        char gender='M';
        char symbol='*';
        char numberChar='1';


        /*
        String data type belongs to a different category called non-primitives that's why for String rules are a
        little different for examples S of String is upper case its color is also not blue
         */

        String name="Sam";

        boolean hungry=true; // true means yes
        boolean sleepy=false; // false means no


        }
}
